[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scraper-engine"
version = "0.1.0"
description = "A reusable project for fetching, parsing, logging and storage data via webscraping."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Alessandro Ciapetti" }]
dependencies = [
    "bs4>=0.0.2",
    "curl-cffi>=0.15.0",
    "extruct>=0.18.0",
    "psycopg2>=2.9.12",
    "pyyaml>=6.0.3",
    "sqlalchemy>=2.0.52",
]

[tool.setuptools]
package-dir = {"" = "src"}
packages = [
    "scraper_engine",
    "scraper_engine.fetchers",
    "scraper_engine.handlers",
    "scraper_engine.loggers",
    "scraper_engine.loggers.configs",
    "scraper_engine.parsers",
    "scraper_engine.storage",
]

[tool.setuptools.package-data]
scraper_engine = ["loggers/*.yaml"]
